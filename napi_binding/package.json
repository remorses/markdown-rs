{
  "name": "markdown-napi",
  "version": "0.1.0",
  "scripts": {
    "build": "napi build --platform",
    "test": "vitest run"
  },
  "devDependencies": {
    "@napi-rs/cli": "^2.16.1",
    "vitest": "^1.5.0"
  }
}
